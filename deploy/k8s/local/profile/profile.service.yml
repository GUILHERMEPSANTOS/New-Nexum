apiVersion: v1
kind: Service
metadata:
  name: profile
spec:
 selector:
   app.kubernetes.io/name: profile
 type: LoadBalancer 
 ports:
  - name: http
    protocol: TCP
    port: 7000
    targetPort: profile-port
    
    
