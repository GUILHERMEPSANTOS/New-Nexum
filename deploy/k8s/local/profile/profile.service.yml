apiVersion: v1
kind: Service
metadata:
  name: profile
spec:
 selector:
   app.kubernetes.io/name: profile
 type: NodePort 
 ports:
  - name: http
    protocol: TCP
    port: 7000
    targetPort: profile-port
    nodePort: 30080
    
